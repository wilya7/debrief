[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "debrief"
version = "1.1.0"
description = "AI-powered presentation assistant for scientists: generate structured slide decks from research content via Claude Code."
authors = [
    { name = "Carlo Fusco and Leonardo Restivo" },
]
license = { text = "Apache-2.0" }
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "playwright>=1.40",
    "python-pptx>=0.6.21",
    "PyMuPDF>=1.23",
    "json-repair>=0.25",
]

# Optional dependencies (BUG-AUDIT-103 / BC-1.18a). The `anthropic` SDK is no
# longer required for any canonical user flow — cycles 101 and 102 routed the
# rewriter and script-writer through Claude Code's Task tool, which uses the
# session credential (OAuth or API key). The SDK is only used by legacy
# direct-CLI paths (`python -m debrief.launcher script_writer` invoked
# directly, the `--trigger /debrief:handout-cascade` choice,
# `main_rewrite_brief`'s synthesis branch). Users who want those paths install
# with `pip install '.[sdk_fallback]'`.
[project.optional-dependencies]
sdk_fallback = ["anthropic>=0.40"]

# NOTE: Intentionally NO [project.scripts] entry.
# bin/debrief is the single entry point (spec §9.4 line 683, BC-1.17). A
# console_script shim installed by pip would bypass the conda bootstrap in
# spec §24.4 and break first-run setup. See BUG-AUDIT-1.

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
"debrief" = ["py.typed"]

[tool.ruff]
line-length = 88
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "W", "I", "N", "UP", "B", "C4", "SIM"]
ignore = ["E501"]

[tool.ruff.lint.isort]
known-first-party = ["debrief"]

[tool.mypy]
python_version = "3.11"
strict = true
warn_return_any = true
warn_unused_configs = true
ignore_missing_imports = true
exclude = ["tests/"]

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
python_classes = ["Test*"]
python_functions = ["test_*"]
addopts = "--tb=short"

[tool.coverage.run]
source = ["src/debrief"]
omit = ["tests/*"]

[tool.coverage.report]
fail_under = 80
